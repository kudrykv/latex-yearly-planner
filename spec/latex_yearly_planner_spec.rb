# frozen_string_literal: true

RSpec.describe LatexYearlyPlanner do
  it 'has a version number' do
    expect(LatexYearlyPlanner::VERSION).not_to be_nil
  end

  it 'test' do
    expect(1).to be(2), '1 is not 2'
  end
end
